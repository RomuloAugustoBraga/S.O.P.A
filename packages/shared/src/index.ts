/**
 * Placeholder de smoke-test: valida que o pipeline pnpm workspace + tsc
 * (project references) + Biome está funcionando de ponta a ponta.
 * Será substituído por tipos e utilitários reais compartilhados
 * entre apps/api e apps/desktop a partir da Etapa 2.
 */
export const SHARED_PACKAGE_VERSION = '0.0.0';
